{
    "version": 2,
    "builds": [
      {
        "src": "package.json",
        "use": "@vercel/next"
      }
    ],
    "routes": [
      {
        "src": "/api/auth/(.*)",
        "dest": "/api/auth/[...all]"
      },
      {
        "src": "/(.*)",
        "dest": "/"
      }
    ]
  }